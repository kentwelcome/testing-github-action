name: 'Testing GitHub Action Marketplace in CI'

on: 
    - workflow_dispatch
    - push
    - pull_request

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Testing Github Action Marketplace
      uses: kentwelcome/testing-github-action@v1.1
      with:
        api_host: 'https://44cae958008b.ngrok-free.app'